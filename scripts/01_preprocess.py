# Load dataset
# Drop unnecessary columns (e.g., "Serial No.")
# Rename columns for consistency (e.g., remove spaces)
# Handle missing values (if any)
# Normalize or scale numerical features if necessary
# Save the cleaned dataset to outputs/
